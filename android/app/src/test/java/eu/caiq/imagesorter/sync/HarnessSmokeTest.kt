package eu.caiq.imagesorter.sync

import org.junit.Assert.assertEquals
import org.junit.Test

/** Proves the JVM unit-test source set is wired and runs green. */
class HarnessSmokeTest {

    @Test
    fun harnessRunsAtLeastOneTest() {
        assertEquals(4, 2 + 2)
    }
}
